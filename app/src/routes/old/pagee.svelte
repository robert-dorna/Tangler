

<!-- <ItemModal
  bind:visible={modalVisible}
  onSubmit={() => (modalMethod === "create" ? create() : update())}
  itemtype={modalItemtype}
  bind:item={modalItem}
  source={modalSource}
  edited={modalEdited}
/> -->